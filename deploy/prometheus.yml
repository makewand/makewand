global:
  scrape_interval: 15s

scrape_configs:
  - job_name: makewand
    static_configs:
      - targets: ["127.0.0.1:8080"]
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: replace-with-admin-metrics-token
