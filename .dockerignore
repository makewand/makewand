.git
.github
.claude

# Secrets — keep provider keys / server tokens out of the build context.
# The context is the repo root (deploy/docker-compose.yml) and the builder
# runs `COPY . .`, so an unignored .env would land in a build-stage layer.
.env
deploy/.env

# Local build outputs
build/
buildtest
modetest
mwtest
qualtest
versus
makewand
makewand.exe

# Local/generated work directories
benchmarks/results/
blog-project/
notes-api/
output/
password-strength-checker/
top_three_project/

# Tool caches and editor noise
.pytest_cache/
**/__pycache__/
**/node_modules/
.DS_Store
